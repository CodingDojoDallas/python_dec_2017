when using sum, avg, count; group by non-aggregated data comma-separated

JOIN states
ON cities.state_id = states.id

If doing a select statement using a querydb method, you will be returning a list of dictionaries